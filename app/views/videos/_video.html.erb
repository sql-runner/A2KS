<li>
  <fieldset>
    <%= image_tag video.media.url(:thumb) %>
    Video:  <%= link_to video.name, video_path(video) %>
    Description: <%= video.description %>
    Tags: <%= video.tag_list %>
  </fieldset>
</li>
